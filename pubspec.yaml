name: utm_fire_extinguisher_scanner
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  simple_animations: ^1.3.3
  cupertino_icons: ^0.1.3
  flutter_barcode_scanner: ^1.0.1

  #Firebase
  firebase_core: ^1.7.0
  firebase_auth: ^3.1.3
  font_awesome_flutter: ^9.1.0
  google_fonts: ^2.1.0

  provider: ^4.3.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
